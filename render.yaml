services:
  - type: web
    name: shipmate-logistics
    env: static
    buildCommand: ""
    staticPublishPath: "."
    routes:
      # Redirect old .html URLs to clean trailing slash URLs
      - type: redirect
        source: /about.html
        destination: /about/
      - type: redirect
        source: /services.html
        destination: /services/
      - type: redirect
        source: /fleet.html
        destination: /fleet/
      - type: redirect
        source: /contact.html
        destination: /contact/
      - type: redirect
        source: /index.html
        destination: /

      # Redirect non-slash URLs to trailing slash URLs
      - type: redirect
        source: /about
        destination: /about/
      - type: redirect
        source: /services
        destination: /services/
      - type: redirect
        source: /fleet
        destination: /fleet/
      - type: redirect
        source: /contact
        destination: /contact/